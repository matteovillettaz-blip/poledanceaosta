---
const values = [
  {
    icon: 'star',
    title: 'Eccellenza',
    description: 'Insegnanti certificate e campionesse con esperienza internazionale'
  },
  {
    icon: 'heart',
    title: 'Accoglienza',
    description: 'Un ambiente inclusivo dove ogni persona puo esprimere se stessa'
  },
  {
    icon: 'users',
    title: 'Community',
    description: 'Una famiglia di appassionate che si supportano a vicenda'
  },
  {
    icon: 'trending-up',
    title: 'Crescita',
    description: 'Percorsi personalizzati per ogni livello, dai primi passi alle competizioni'
  }
];
---

<section class="about section" id="chi-siamo">
  <div class="container">
    <!-- Header -->
    <div class="section__header" data-animate="fade-up">
      <span class="section__subtitle">Chi Siamo</span>
      <h2 class="section__title">Il Primo Studio di<br/>Pole Dance in Valle d'Aosta</h2>
    </div>

    <!-- Main Content -->
    <div class="about__content">
      <!-- Text Side -->
      <div class="about__text" data-animate="fade-up">
        <p class="about__lead">
          Pole Dance Aosta nasce nel <strong>2019</strong> dalla passione di <strong>Simona Spataro</strong>, campionessa italiana e finalista ai mondiali.
        </p>
        <p class="about__description">
          Il nostro studio e uno spazio di <strong>6 pali professionali</strong> dove praticare a ogni eta, dalla pausa pranzo alla sera. Un ambiente accogliente dove ogni persona puo esplorare il movimento, la forza e l'espressivita del proprio corpo.
        </p>
        <p class="about__description">
          Non serve esperienza pregressa: che tu voglia tenerti in forma, sfidare i tuoi limiti o prepararti per le competizioni, qui troverai il percorso giusto per te.
        </p>
        <div class="about__cta">
          <a href="#contatti" class="btn btn--primary">
            Inizia il Tuo Percorso
          </a>
        </div>
      </div>

      <!-- Image Side -->
      <div class="about__image" data-animate="fade-up">
        <div class="about__image-wrapper">
          <img
            src="https://images.unsplash.com/photo-1518611012118-696072aa579a?w=600&h=800&fit=crop"
            alt="Studio Pole Dance Aosta"
            loading="lazy"
          />
          <div class="about__image-accent"></div>
        </div>
        <!-- Experience Badge -->
        <div class="about__badge">
          <span class="about__badge-number">5+</span>
          <span class="about__badge-text">Anni di<br/>Esperienza</span>
        </div>
      </div>
    </div>

    <!-- Values Grid -->
    <div class="about__values">
      {values.map((value, index) => (
        <div class="about__value" data-animate="fade-up" style={`--delay: ${index * 0.1}s`}>
          <div class="about__value-icon">
            {value.icon === 'star' && (
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"></polygon>
              </svg>
            )}
            {value.icon === 'heart' && (
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path>
              </svg>
            )}
            {value.icon === 'users' && (
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
                <circle cx="9" cy="7" r="4"></circle>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
              </svg>
            )}
            {value.icon === 'trending-up' && (
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"></polyline>
                <polyline points="17 6 23 6 23 12"></polyline>
              </svg>
            )}
          </div>
          <h3 class="about__value-title">{value.title}</h3>
          <p class="about__value-description">{value.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .about {
    background-color: var(--bg-primary);
  }

  /* Content Grid */
  .about__content {
    display: grid;
    gap: var(--space-12);
    margin-bottom: var(--space-20);
  }

  @media (min-width: 1024px) {
    .about__content {
      grid-template-columns: 1fr 1fr;
      gap: var(--space-16);
      align-items: center;
    }
  }

  /* Text */
  .about__lead {
    font-family: var(--font-accent);
    font-size: var(--text-xl);
    font-weight: var(--font-regular);
    line-height: var(--leading-relaxed);
    color: var(--text-primary);
    margin-bottom: var(--space-6);
  }

  .about__lead strong {
    color: var(--color-accent-dark);
    font-weight: var(--font-medium);
  }

  .about__description {
    font-size: var(--text-base);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
  }

  .about__description strong {
    color: var(--text-primary);
  }

  .about__cta {
    margin-top: var(--space-8);
  }

  /* Image */
  .about__image {
    position: relative;
  }

  .about__image-wrapper {
    position: relative;
    border-radius: var(--radius-2xl);
    overflow: hidden;
  }

  .about__image-wrapper img {
    width: 100%;
    height: auto;
    aspect-ratio: 3/4;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .about__image-wrapper:hover img {
    transform: scale(1.05);
  }

  .about__image-accent {
    position: absolute;
    top: var(--space-4);
    right: var(--space-4);
    bottom: var(--space-4);
    left: var(--space-4);
    border: 2px solid var(--color-accent);
    border-radius: var(--radius-xl);
    pointer-events: none;
  }

  .about__badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 120px;
    height: 120px;
    background: var(--gradient-gold);
    border-radius: var(--radius-full);
    box-shadow: var(--shadow-xl);
    animation: float 3s ease-in-out infinite;
  }

  @media (max-width: 768px) {
    .about__badge {
      right: 10px;
      bottom: -10px;
      width: 100px;
      height: 100px;
    }
  }

  .about__badge-number {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: var(--font-bold);
    color: var(--color-primary);
    line-height: 1;
  }

  .about__badge-text {
    font-size: var(--text-xs);
    font-weight: var(--font-semibold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
    text-align: center;
    color: var(--color-primary);
    line-height: var(--leading-tight);
  }

  /* Values Grid */
  .about__values {
    display: grid;
    gap: var(--space-6);
    padding-top: var(--space-12);
    border-top: 1px solid var(--bg-tertiary);
  }

  @media (min-width: 768px) {
    .about__values {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-8);
    }
  }

  @media (min-width: 1024px) {
    .about__values {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .about__value {
    text-align: center;
    padding: var(--space-6);
    transition: transform var(--transition-base);
    animation-delay: var(--delay, 0s);
  }

  .about__value:hover {
    transform: translateY(-5px);
  }

  .about__value-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 60px;
    height: 60px;
    background-color: var(--color-accent-light);
    border-radius: var(--radius-full);
    color: var(--color-accent-dark);
    margin-bottom: var(--space-4);
  }

  .about__value-title {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-2);
  }

  .about__value-description {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: 0;
  }
</style>
