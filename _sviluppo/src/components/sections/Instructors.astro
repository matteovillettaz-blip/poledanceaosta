---
const instructors = [
  {
    id: 'simona',
    name: 'Simona Spataro',
    role: 'Fondatrice & Head Instructor',
    image: 'https://images.unsplash.com/photo-1544005313-94ddf0286df2?w=400&h=500&fit=crop&crop=face',
    instagram: 'https://www.instagram.com/simonaspataro_pole/',
    specializations: ['Pole Sport', 'Tricks', 'Workshop'],
    shortBio: 'Campionessa italiana 2017, finalista ai Mondiali Ultrapole 2019. Co-founder di Polescape.',
    fullBio: 'Ginnasta fin dalla tenera eta, si innamora della pole dance nel 2014. Nel 2017 vince il 1 posto all\'Italian Pole Dance Contest, partecipa a gare nazionali e internazionali fino ai Mondiali Ultrapole 2019 in Canada. Ama combinare elementi di flow a tricks dinamici. Nel 2019 fonda Pole Dance Aosta e tiene workshop in tutta Italia. Co-founder di Polescape, pole retreats in giro per il mondo.',
    achievements: ['Campionessa Italiana 2017', 'Finalista Mondiali 2019']
  },
  {
    id: 'nicole',
    name: 'Nicole Carere',
    role: 'Istruttrice Pole Dance & Exotic',
    image: 'https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=400&h=500&fit=crop&crop=face',
    instagram: 'https://www.instagram.com/nicolecarere_pole/',
    specializations: ['Exotic', 'Floorwork', 'Coreografie'],
    shortBio: 'Plurimedagliata in competizioni nazionali di pole dance e pole exotic.',
    fullBio: 'Background nella ginnastica artistica, ha conquistato diverse medaglie in competizioni di pole dance e pole exotic a livello nazionale. La sua energia contagiosa e la passione per l\'insegnamento rendono ogni lezione un\'esperienza unica. Specializzata in exotic e floorwork.',
    achievements: ['Multiple medaglie nazionali']
  },
  {
    id: 'federica',
    name: 'Federica Armand',
    role: 'Istruttrice Pole Dance & Exotic',
    image: 'https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=400&h=500&fit=crop&crop=face',
    instagram: 'https://www.instagram.com/federicaarmand_pole/',
    specializations: ['Pole Dance', 'Flessibilita', 'Beginners'],
    shortBio: 'Atleta e insegnante, guida allieve di ogni livello con pazienza e professionalita.',
    fullBio: 'Con un solido background nella ginnastica, ha vinto diverse medaglie nelle categorie pole dance e pole exotic. La sua pazienza e attenzione ai dettagli la rendono perfetta per guidare allieve di ogni livello, dalle principianti alle avanzate. Ama creare coreografie che valorizzino lo stile personale di ogni allieva.',
    achievements: ['Medaglie in pole dance e exotic']
  }
];
---

<section class="instructors section" id="insegnanti">
  <div class="container">
    <!-- Header -->
    <div class="section__header" data-animate="fade-up">
      <span class="section__subtitle">Il Nostro Team</span>
      <h2 class="section__title">Campionesse e Professioniste<br/>Pronte a Guidarti</h2>
      <p class="section__description">
        Insegnanti certificate con esperienza nazionale e internazionale, unite dalla passione per la pole dance e l'insegnamento.
      </p>
    </div>

    <!-- Instructors Grid -->
    <div class="instructors__grid">
      {instructors.map((instructor, index) => (
        <article
          class="instructor-card"
          data-instructor={instructor.id}
          data-animate="fade-up"
          style={`--delay: ${index * 0.15}s`}
        >
          <!-- Image -->
          <div class="instructor-card__image-wrapper">
            <img
              src={instructor.image}
              alt={instructor.name}
              class="instructor-card__image"
              loading="lazy"
            />
            <div class="instructor-card__overlay">
              <a
                href={instructor.instagram}
                target="_blank"
                rel="noopener noreferrer"
                class="instructor-card__instagram"
                aria-label={`Instagram di ${instructor.name}`}
              >
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
                  <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
                  <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
                </svg>
              </a>
            </div>
            {instructor.achievements && (
              <div class="instructor-card__badge">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                </svg>
              </div>
            )}
          </div>

          <!-- Content -->
          <div class="instructor-card__content">
            <div class="instructor-card__header">
              <h3 class="instructor-card__name">{instructor.name}</h3>
              <p class="instructor-card__role">{instructor.role}</p>
            </div>

            <!-- Specializations -->
            <div class="instructor-card__specs">
              {instructor.specializations.map(spec => (
                <span class="instructor-card__spec">{spec}</span>
              ))}
            </div>

            <!-- Bio -->
            <div class="instructor-card__bio">
              <p class="instructor-card__bio-short">{instructor.shortBio}</p>
              <p class="instructor-card__bio-full">{instructor.fullBio}</p>
            </div>

            <!-- Achievements -->
            {instructor.achievements && (
              <div class="instructor-card__achievements">
                {instructor.achievements.map(achievement => (
                  <span class="instructor-card__achievement">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                    {achievement}
                  </span>
                ))}
              </div>
            )}

            <!-- Toggle -->
            <button class="instructor-card__toggle" aria-expanded="false">
              <span class="toggle-text">Leggi di piu</span>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="6 9 12 15 18 9"></polyline>
              </svg>
            </button>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .instructors {
    background-color: var(--bg-primary);
  }

  /* Grid */
  .instructors__grid {
    display: grid;
    gap: var(--space-8);
  }

  @media (min-width: 768px) {
    .instructors__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .instructors__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* Card */
  .instructor-card {
    background-color: var(--bg-primary);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-md);
    transition: all var(--transition-base);
    animation-delay: var(--delay, 0s);
  }

  .instructor-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-xl);
  }

  /* Image */
  .instructor-card__image-wrapper {
    position: relative;
    overflow: hidden;
  }

  .instructor-card__image {
    width: 100%;
    aspect-ratio: 4/5;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .instructor-card:hover .instructor-card__image {
    transform: scale(1.05);
  }

  .instructor-card__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 50%,
      rgba(26, 26, 26, 0.7) 100%
    );
    display: flex;
    align-items: flex-end;
    justify-content: flex-end;
    padding: var(--space-4);
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .instructor-card:hover .instructor-card__overlay {
    opacity: 1;
  }

  .instructor-card__instagram {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    background-color: var(--bg-primary);
    border-radius: var(--radius-full);
    color: var(--text-primary);
    transition: all var(--transition-fast);
  }

  .instructor-card__instagram:hover {
    background-color: var(--color-accent);
    transform: scale(1.1);
  }

  .instructor-card__badge {
    position: absolute;
    top: var(--space-4);
    right: var(--space-4);
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: var(--gradient-gold);
    border-radius: var(--radius-full);
    color: var(--color-primary);
  }

  /* Content */
  .instructor-card__content {
    padding: var(--space-6);
  }

  .instructor-card__header {
    margin-bottom: var(--space-4);
  }

  .instructor-card__name {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--font-semibold);
    color: var(--text-primary);
    margin-bottom: var(--space-1);
  }

  .instructor-card__role {
    font-size: var(--text-sm);
    color: var(--color-accent-dark);
    font-weight: var(--font-medium);
    margin-bottom: 0;
  }

  /* Specializations */
  .instructor-card__specs {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
  }

  .instructor-card__spec {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    font-size: var(--text-xs);
    font-weight: var(--font-medium);
    color: var(--text-secondary);
    background-color: var(--bg-secondary);
    border-radius: var(--radius-full);
  }

  /* Bio */
  .instructor-card__bio {
    margin-bottom: var(--space-4);
  }

  .instructor-card__bio-short {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: 0;
  }

  .instructor-card__bio-full {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin-bottom: 0;
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: all var(--transition-base);
  }

  .instructor-card.is-expanded .instructor-card__bio-short {
    display: none;
  }

  .instructor-card.is-expanded .instructor-card__bio-full {
    max-height: 300px;
    opacity: 1;
  }

  /* Achievements */
  .instructor-card__achievements {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    margin-bottom: var(--space-4);
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: all var(--transition-base);
  }

  .instructor-card.is-expanded .instructor-card__achievements {
    max-height: 200px;
    opacity: 1;
    margin-top: var(--space-4);
    padding-top: var(--space-4);
    border-top: 1px solid var(--bg-tertiary);
  }

  .instructor-card__achievement {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-xs);
    color: var(--text-secondary);
  }

  .instructor-card__achievement svg {
    color: var(--color-gold);
  }

  /* Toggle */
  .instructor-card__toggle {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    color: var(--color-accent-dark);
    cursor: pointer;
    transition: color var(--transition-fast);
  }

  .instructor-card__toggle:hover {
    color: var(--color-primary);
  }

  .instructor-card__toggle svg {
    transition: transform var(--transition-base);
  }

  .instructor-card.is-expanded .instructor-card__toggle svg {
    transform: rotate(180deg);
  }

  .instructor-card.is-expanded .instructor-card__toggle .toggle-text {
    font-size: 0;
  }

  .instructor-card.is-expanded .instructor-card__toggle .toggle-text::before {
    content: 'Mostra meno';
    font-size: var(--text-sm);
  }
</style>

<script>
  document.querySelectorAll('.instructor-card__toggle').forEach(toggle => {
    toggle.addEventListener('click', () => {
      const card = toggle.closest('.instructor-card');
      const isExpanded = card?.classList.toggle('is-expanded');
      toggle.setAttribute('aria-expanded', String(isExpanded));
    });
  });
</script>
